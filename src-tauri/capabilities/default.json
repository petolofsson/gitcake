{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all windows",
  "windows": ["widget", "main", "preferences"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
